# PaperLens 项目总结（阶段性收尾）

## 1. 项目概况

PaperLens 是"证据可追溯的论文阅读辅助 Agent"：上传 PDF 或 arXiv 链接后，系统解析出结构化的论文（章节、段落、图表、公式、参考文献），提供双语阅读、证据可追溯的问答、质量评估、多篇对比与参考文献规范检查。技术栈：Python（FastAPI + SQLite）+ Next.js 16 + DeepSeek API。

**课题要求的五项内容全部实现**：章节识别、结构概览与段落检索问答、质量评估、多篇方法/数据/指标对比、参考文献提取与格式检查。

## 2. 迭代历程（问题与方法）

### 2.1 解析层

- **起点**：pdfplumber 逐词提取 + 固定间隙拼行 → 双栏/公式/旋转文字问题频出。
- **演进**：PyMuPDF span 级几何提取（字体/基线/方向），旋转文字过滤，表格区域掩膜（表格不污染正文），页级质量门双引擎融合（低质页自动换引擎），arXiv LaTeXML HTML Source-first（结构化优先，老论文回退 PDF）。
- **收尾修复**：PyMuPDF 表格区域只掩膜不占位导致表格文字丢失——补 TABLE 占位块，资产链路打通。

### 2.2 检索与问答

- 段落感知分片 + 自研 BM25（课程要求"自行实现检索"）。
- 幻觉治理：证据账本 → 主张生成受确定性守卫约束（引文逐字/数字/否定/比较词）→ 每主张独立 LLM 并发核验 → 只把通过核验的主张组织成答案。
- 性能：draft 25.3s → 14.4s（紧凑 JSON 指令 + 无用字段移出 schema），会话缓存二次提问 1.1s。

### 2.3 翻译

- 渐进式翻译（视口触发 + 批次并发 + 逐批持久化），术语表一致性（漂移检测 + 选择性修复），公式/引用占位保护。
- 处理过的问题：按 block_id 缓存翻译在重解析后错位——内容前缀比对（unit_matches_block）修复；纯数字引用被误判丢失——CIT 校验修复。

### 2.4 多篇比较

- 主题对齐 → 字段抽取（artifact 字段复用 + LLM 补缺）→ 结构化结果对比。
- 处理过的问题：13 字段全开信息堆积（收敛默认 5 字段）；自动最佳值判定不可靠（行识别、指标方向）——从"自动排名"调整为"结构化展示 + 同条件才可对比"。
- 性能：串行抽取 → 并发抽取（90s 出 26 格）。

### 2.5 参考文献

- 双路径提取 → 风格感知格式检查（13 种问题）→ Crossref/arXiv 在线核验（瀑布式匹配）。
- 处理过的问题：年份误抓 arXiv 编号；长作者列表标题解析失败；精确命中但本地字段不全时无法认证的问题（放宽为 PROBABLE + 回填）。
- 批量核验：后台线程 + 4 并发 + 边核验边更新（244 条约 4 分钟）。

## 3. 工程化沉淀

- **测试**：72 项离线测试（无网络/LLM 依赖），覆盖解析、检索、核验、比较、端点契约。
- **部署**：systemd + nginx，mihomo 代理解决 arXiv 国内直连；发布包守卫（防 .env/密钥/数据库/版权 PDF 进包）。
- **性能基线**：HTML fetch 54s→2s、翻译 26s→10s、批量核验 18min→4min、比较 2-4min→90s。

## 4. 未解决问题（后续路线）

1. 复杂表格（无边框/跨页）与公式 OCR——接 Docling/GROBID/视觉模型。
2. Embedding + RRF 融合（先建召回评测）。
3. 自动数值比较的可靠性（ComparabilityKey 指标本体库 + 人工确认"本方法行"）。
4. 长期用户偏好记忆、Skill 运行时加载、HITL 审批真正接入产品。
5. 云端化：独立 worker、PostgreSQL/pgvector、认证租户隔离。

## 5. 一句话总结

> 项目遵循"先理解论文结构，再检索证据，最后在证据约束下生成"的主线——确定性解析构建文档实体，可解释检索与证据核验保证可信，翻译、问答、比较与质量评估均建立在同一套文档实体与证据定位机制之上。
