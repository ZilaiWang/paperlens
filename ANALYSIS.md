# PaperLens 边界分析

## 1. 方案对比与选型依据

### 1.1 解析层：自研 vs 外部工具（GROBID/Docling）

| 方案 | 优点 | 缺点 | 结论 |
| --- | --- | --- | --- |
| GROBID | 学术级结构化 | 需部署 Java 服务、模型重、无国内加速 | 后续路线 |
| Docling | 版面分析全面 | 依赖重、视觉模型按页成本高 | 后续路线 |
| 自研 PyMuPDF+pdfplumber | 轻量、可控、可解释 | 表格识别粗、无视觉模型 | ✅ 采用（课程要求自行实现章节识别） |

**结论**：课程要求"自行实现章节识别"，自研方案在 2C2G 单机上可行且可解释；外部工具留作后续增强。

### 1.2 检索层：自研 BM25 vs Embedding

- 自研 BM25：零依赖、可离线运行、检索过程可解释，且满足课程语料规模的需求。
- Embedding 向量检索：语义召回更强，但需要模型服务与向量库。
- **结论**：本阶段 BM25；P2 路线为 embedding + RRF 融合（先建召回评测，以评测结果为依据逐步替换）。

### 1.3 答案生成：证据约束 vs 自由生成

- 自由生成（直接把全文给 LLM）：快但幻觉不可控、无证据定位。
- 证据约束（本方案）：先检索候选证据 → LLM 只在证据边界内生成主张 → 逐主张语义核验 → 失败引文自动修正/丢弃。
- **结论**：证据约束，牺牲一点速度换可追溯性（质量评估与多篇比较复用同一证据定位机制）。

## 2. 已识别问题与处理（审计 P0/P1）

| 问题 | 处理 |
| --- | --- |
| PyMuPDF 表格区域被排除但无 TABLE 占位（表格文字消失、资产为 0） | 适配器按表格 bbox 生成 `⟦TABLE⟧` 占位块，贯穿段落重建→章节→资产（修复后实测 21 个表格占位） |
| 年份误抓 arXiv 编号（"arXiv:1904.04232" 的 1904 当年份） | YEAR_RE 加 `(?!\d|\.\d)` 断言，PDF/HTML 两路径统一 |
| 长作者列表（逗号分隔）标题解析为空 | 标题启发式多级回退：and/et al. 收尾标记识别 + 句点分段 |
| 精确 ID 命中但本地字段不全时无法认证 | 放宽为 PROBABLE 并回填注册元数据；VERIFIED 仍要求全字段一致 |
| 多篇比较 13 字段信息堆积 | 默认收敛为 5 个核心字段 |
| 自动数值最佳值判定不可靠（行识别/指标方向/条件通配） | 前端隐藏 ★，只保留"同条件可对比"描述性结论 |
| 比较创建后 GET 404 导致前端轮询地址变 undefined 卡死 | POST 时立即持久化 RUNNING 记录 + 前端保留原 id 轮询 |
| 翻译全局并发无上限 | 进程级 Semaphore(4) |
| 纯图片 PDF 章节识别 body_font 空 Counter 崩溃 | 空 Counter 时 body_font=None |

## 3. 已知边界

- **自动数值比较**：自动最佳值判定已隐藏，原因是无法可靠识别"哪一行是论文自己的方法"及指标方向（误差/FLOPs 越小越好）。当前只做结构化展示，不自动排名。
- **HTML 论文无物理分页**：目录不显示页码；PDF 与 HTML 双路径的页码语义不同，未强行统一。
- **无 ID 参考文献**：无 DOI/arXiv 编号且模糊搜索低分的条目保持 UNRESOLVED（数据本身限制，非缺陷）。
- **长期记忆 / Skill / HITL**：有接口设计（SQLiteLongTermStore、ConsentWorkflow 等），未接入当前产品链路，列为后续工程化工作。
- **云端化**：单机形态（SQLite + 单进程线程 + systemd），PostgreSQL/Redis/Celery 为 P2 路线。

## 4. 性能数据（线上实测）

| 环节 | 优化前 | 优化后 |
| --- | --- | --- |
| arXiv HTML fetch | 24–54s（直连挂起） | ~2s（mihomo 代理） |
| 原版 PDF 下载 | 首次打开现场下载（数秒-几十秒） | 导入期与解析并行后台预下载 |
| 多篇比较（2 篇） | 串行抽取 2–4 分钟 | 并发抽取 90s 出 26 格 |
| 批量核验（244 条） | 串行约 18 分钟 | 4 并发约 4 分钟 + 边核验边更新 |
| 问答草稿生成 | 25.3s | 14.4s（紧凑 JSON 指令）+ 会话缓存二次 1.1s |
